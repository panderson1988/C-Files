#include <stdio.h>
#include <string.h>

void minimum(long *xptr) {
	 
	*xptr = ~*xptr<<63; //this is printing the hex and decimal to an int
	//Instructions mentions constants 0-63
}


int main() {
  char c; 
	{
	return c >= 'a' && c <= 'z';
	}
}